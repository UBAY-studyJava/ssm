<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.mvc.cartmapper"> <!-- Repository와 연동을 위해 사용될 namespace -->
	<insert id="trans_action" parameterType="trans_action"> <!-- trans_action클래스를 변수로 받아와 쿼리에 사용 -->
		insert
		into cart(user_name, balance, account, regidate)
		values (#{user_name}, #{balance}, #{account_number}, #{regidate})
	</insert>
	
	<select id="trans_action" resultType="trans_action"> <!-- 조회된 값을 trans_action클래스에 담아 return -->
		select user_name, balance, account_number, regidate
		from trans_action
	</select>
</mapper>