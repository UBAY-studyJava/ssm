<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.soecode.lyf.dao.ProductDao">
	<!-- 目的：为dao接口方法提供sql语句配置 -->
	<!-- 목적: DAO인터페이스 메소드를 위한 sql구문 설정 -->
	<select id="queryById" resultType="Book" parameterType="long">
		<!-- 具体的sql/구체적sql -->
		SELECT
			productId,
			productName,
			productDetail,
			cash,
			balance,
			account_number
		FROM
			saving_product
		WHERE
			producId = #{productId}
	</select>
	
	<select id="queryAll" resultType="Product">
		SELECT
			productId,
			productName,
			productDetail,
			cash,
			balance,
			account_number
		FROM
			saving_product
		ORDER BY
			productId
		LIMIT #{offset}, #{limit}
	</select>
	
	<update id="modifyDetail">
		UPDATE saving_product
		SET
			productName = #{productName},
			productDetail = #{productDetail}
		WHERE
			productId = #{productId}
	</update>
	

	<delete id="delete">
      DELETE from saving_product
      WHERE
         productId = #{productId}
   
   </delete>

	<insert id="insert">  
		INSERT INTO saving_product (productName, productDetail) 
		VALUES (#{productName}, #{productDetail})
   </insert>
	

	
</mapper>